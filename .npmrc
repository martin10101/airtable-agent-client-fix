# Project-local npm config for corporate networks that do SSL inspection.
# Only affects npm commands run from this folder — does NOT change global npm config.
strict-ssl=false
fetch-retries=5
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=120000
